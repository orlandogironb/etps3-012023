import 'package:flutter/material.dart';

class SmoothiesTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Smoothies Tab"),
      ),
    );
  }
}
